[package]
name = "proact"
version = "0.1.0"
edition = "2021"
authors = ["Your Name"]
description = "CLI that generates documentation for AI coding agents"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
chrono = "0.4"
